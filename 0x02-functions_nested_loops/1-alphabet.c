#include <stdio.h>

#include "main.h"

/**
 * main - Entry point
 *
 * Description: Program prints the alphabet in lower case
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	print_alphabet();
	return (0);
}
